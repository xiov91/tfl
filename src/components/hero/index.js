import React from 'react';
import heroImg from './legion.png';

function Hero () {
    return(
            <section>
                <img src={heroImg} className="App-header" alt="legion-banner"/>
            </section>
    );
                
}

export default Hero;